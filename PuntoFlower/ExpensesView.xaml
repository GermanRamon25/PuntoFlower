<UserControl x:Class="PuntoFlower.Views.ExpensesView"
             xmlns:views="clr-namespace:PuntoFlower.Views"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="650" d:DesignWidth="1000"
             Loaded="UserControl_Loaded">

    <UserControl.Resources>
        <Style x:Key="SubTabItemStyle" TargetType="TabItem">
            <Setter Property="HeaderTemplate">
                <Setter.Value>
                    <DataTemplate>
                        <TextBlock x:Name="TabText" Text="{Binding}" Foreground="#5D6D7E" FontWeight="SemiBold" FontSize="14"/>
                        <DataTemplate.Triggers>
                            <DataTrigger Binding="{Binding IsSelected, RelativeSource={RelativeSource AncestorType=TabItem}}" Value="True">
                                <Setter TargetName="TabText" Property="Foreground" Value="#CF8282"/>
                                <Setter TargetName="TabText" Property="FontWeight" Value="Bold"/>
                            </DataTrigger>
                        </DataTemplate.Triggers>
                    </DataTemplate>
                </Setter.Value>
            </Setter>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TabItem">
                        <Border x:Name="Border" Margin="0,0,8,0" Padding="20,10" CornerRadius="8" Background="#E5E8E8" BorderThickness="0" Cursor="Hand">
                            <ContentPresenter x:Name="ContentSite" VerticalAlignment="Center" HorizontalAlignment="Center" ContentSource="Header"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="White"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="#F8F9FA"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Grid Background="#F5F5F5">
        <StackPanel Margin="20">
            <TextBlock x:Name="lblTituloGastos" Text="Gastos y Surtido de Mercancía" FontSize="24" FontWeight="Bold" Margin="0,0,0,15" Foreground="#2C3E50"/>

            <TabControl x:Name="tcGastosPrincipal" Background="Transparent" BorderThickness="0">

                <TabItem Header="Pago de Servicios" Style="{StaticResource SubTabItemStyle}">
                    <StackPanel Margin="0,15,0,0">

                        <Border Background="White" CornerRadius="10" Padding="20" Margin="0,0,0,15">
                            <Border.Effect>
                                <DropShadowEffect BlurRadius="10" ShadowDepth="1" Color="#DDD"/>
                            </Border.Effect>
                            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                <StackPanel Margin="0,0,15,0">
                                    <TextBlock Text="Concepto / Servicio" Margin="0,0,0,5" Foreground="#7F8C8D"/>
                                    <ComboBox x:Name="cbConceptoServicio" Width="180" Height="30" VerticalContentAlignment="Center" SelectedIndex="0" SelectionChanged="cbConceptoServicio_SelectionChanged">
                                        <ComboBoxItem Content="Luz (CFE)"/>
                                        <ComboBoxItem Content="Agua"/>
                                        <ComboBoxItem Content="Telmex"/>
                                        <ComboBoxItem Content="Comida de las floristas"/>
                                        <ComboBoxItem Content="Ley"/>
                                        <ComboBoxItem Content="Papelerama"/>
                                        <ComboBoxItem Content="Otro Servicio / Gasto"/>
                                    </ComboBox>
                                </StackPanel>

                                <StackPanel x:Name="panelOtroConcepto" Margin="0,0,15,0" Visibility="Collapsed">
                                    <TextBlock Text="Escriba el Concepto" Margin="0,0,0,5" Foreground="#E67E22" FontWeight="SemiBold"/>
                                    <TextBox x:Name="txtDesc" Width="180" Height="30" VerticalContentAlignment="Center"/>
                                </StackPanel>

                                <StackPanel Margin="0,0,15,0">
                                    <TextBlock Text="Monto ($)" Margin="0,0,0,5" Foreground="#7F8C8D"/>
                                    <TextBox x:Name="txtMonto" Width="90" Height="30" VerticalContentAlignment="Center"/>
                                </StackPanel>

                                <StackPanel x:Name="panelCategoria" Margin="0,0,15,0">
                                    <TextBlock Text="Categoría" Margin="0,0,0,5" Foreground="#7F8C8D"/>
                                    <ComboBox x:Name="cbCategoria" Width="150" Height="30" VerticalContentAlignment="Center">
                                        <ComboBoxItem Content="Renta / Servicios" IsSelected="True"/>
                                        <ComboBoxItem Content="Pago Empleada"/>
                                        <ComboBoxItem Content="Bodega Floral (Insumos)"/>
                                        <ComboBoxItem Content="Material de Limpieza"/>
                                        <ComboBoxItem Content="Pago de Material"/>
                                    </ComboBox>
                                </StackPanel>

                                <StackPanel Margin="0,0,15,0">
                                    <TextBlock Text="Método Pago" Margin="0,0,0,5" Foreground="#7F8C8D"/>
                                    <ComboBox x:Name="cbMetodoGasto" Width="120" Height="30" VerticalContentAlignment="Center">
                                        <ComboBoxItem x:Name="cbiEfectivo" Content="Efectivo" IsSelected="True"/>
                                        <ComboBoxItem x:Name="cbiTarjeta" Content="Tarjeta"/>
                                        <ComboBoxItem x:Name="cbiTransferencia" Content="Transferencia"/>
                                    </ComboBox>
                                </StackPanel>

                                <Button Content="Registrar Gasto" Click="btnRegistrarGasto_Click" Background="#27AE60" Foreground="White" 
                                        Padding="20,0" Height="33" VerticalAlignment="Bottom" FontWeight="Bold" BorderThickness="0" Cursor="Hand">
                                    <Button.Resources>
                                        <Style TargetType="Border">
                                            <Setter Property="CornerRadius" Value="6"/>
                                        </Style>
                                    </Button.Resources>
                                </Button>
                            </StackPanel>
                        </Border>

                        <Border Background="White" CornerRadius="10" Padding="15">
                            <Border.Effect>
                                <DropShadowEffect BlurRadius="10" ShadowDepth="1" Color="#DDD"/>
                            </Border.Effect>
                            <DockPanel>
                                <Grid DockPanel.Dock="Top" Margin="0,0,0,10">
                                    <TextBlock Text="Historial de Egresos Aplicados" HorizontalAlignment="Left" VerticalAlignment="Center" FontSize="16" FontWeight="Bold" Foreground="#5D6D7E"/>

                                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                                        <TextBlock Text="Desde:" VerticalAlignment="Center" Margin="0,0,5,0" FontSize="13" Foreground="#5D6D7E"/>
                                        <DatePicker x:Name="dpDesdeGastos" Width="115" Height="30" VerticalContentAlignment="Center" Margin="0,0,10,0"/>

                                        <TextBlock Text="Hasta:" VerticalAlignment="Center" Margin="0,0,5,0" FontSize="13" Foreground="#5D6D7E"/>
                                        <DatePicker x:Name="dpHastaGastos" Width="115" Height="30" VerticalContentAlignment="Center" Margin="0,0,15,0"/>

                                        <Button x:Name="btnGenerarReporte" Content="GENERAR REPORTE" Click="btnGenerarReporte_Click" Background="#007ACC" Foreground="White" Padding="12,0" Height="32" FontWeight="Bold" BorderThickness="0" Cursor="Hand" Margin="0,0,10,0">
                                            <Button.Resources>
                                                <Style TargetType="Border">
                                                    <Setter Property="CornerRadius" Value="4"/>
                                                </Style>
                                            </Button.Resources>
                                        </Button>

                                        <Button x:Name="btnImprimirPDF" Content="🖨️ PDF" Click="btnImprimirPDF_Click" Background="#3498DB" Foreground="White" Padding="12,0" Height="32" FontWeight="Bold" BorderThickness="0" Cursor="Hand">
                                            <Button.Resources>
                                                <Style TargetType="Border">
                                                    <Setter Property="CornerRadius" Value="4"/>
                                                </Style>
                                            </Button.Resources>
                                        </Button>
                                    </StackPanel>
                                </Grid>

                                <DataGrid x:Name="dgGastos" AutoGenerateColumns="False" Height="310" 
                                          Background="Transparent" BorderThickness="0" CanUserAddRows="False"
                                          HeadersVisibility="Column" GridLinesVisibility="Horizontal" IsReadOnly="True"
                                          HorizontalGridLinesBrush="#EEE">
                                    <DataGrid.Columns>
                                        <DataGridTextColumn Header="Fecha / Hora" Binding="{Binding Fecha, StringFormat={}{0:dd/MM/yyyy HH:mm}}" Width="140"/>
                                        <DataGridTextColumn Header="Descripción / Concepto" Binding="{Binding Descripcion}" Width="*"/>
                                        <DataGridTextColumn Header="Categoría" Binding="{Binding Categoria}" Width="160"/>
                                        <DataGridTextColumn Header="Método Pago" Binding="{Binding MetodoPago}" Width="120"/>
                                        <DataGridTextColumn Header="Monto" Binding="{Binding Monto, StringFormat={}{0:C}}" Width="100"/>
                                    </DataGrid.Columns>
                                </DataGrid>
                            </DockPanel>
                        </Border>
                    </StackPanel>
                </TabItem>

                <TabItem x:Name="tiComprasProveedores" Header="Compras a Proveedores" Style="{StaticResource SubTabItemStyle}">
                    <Grid Margin="0,15,0,0">
                        <views:ProveedoresView />
                    </Grid>
                </TabItem>

            </TabControl>
        </StackPanel>
    </Grid>
</UserControl>