<UserControl x:Class="PuntoFlower.Views.ConfigurationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" d:DesignHeight="750" d:DesignWidth="1100"
             Loaded="UserControl_Loaded">

    <UserControl.Resources>
        <Style x:Key="SubTabItemStyle" TargetType="TabItem">
            <Setter Property="HeaderTemplate">
                <Setter.Value>
                    <DataTemplate>
                        <TextBlock x:Name="TabText" Text="{Binding}" Foreground="#5D6D7E" FontWeight="SemiBold" FontSize="14"/>
                        <DataTemplate.Triggers>
                            <DataTrigger Binding="{Binding IsSelected, RelativeSource={RelativeSource AncestorType=TabItem}}" Value="True">
                                <Setter TargetName="TabText" Property="Foreground" Value="#CF8282"/>
                                <Setter TargetName="TabText" Property="FontWeight" Value="Bold"/>
                            </DataTrigger>
                        </DataTemplate.Triggers>
                    </DataTemplate>
                </Setter.Value>
            </Setter>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TabItem">
                        <Border x:Name="Border" Margin="0,0,8,0" Padding="20,12" CornerRadius="10" Background="#F2F4F4" BorderThickness="0" Cursor="Hand">
                            <ContentPresenter x:Name="ContentSite" VerticalAlignment="Center" HorizontalAlignment="Center" ContentSource="Header"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="White"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="#FADBD8"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Grid Background="Transparent">
        <StackPanel Margin="20,20,20,10">
            <TextBlock Text="Configuración del Sistema" FontSize="28" FontWeight="Bold" Foreground="#1E312F" Margin="10,0,0,25"/>

            <Border Background="White" CornerRadius="15" Padding="15">
                <Border.Effect>
                    <DropShadowEffect BlurRadius="15" ShadowDepth="2" Color="#D5DBDB" Opacity="0.3"/>
                </Border.Effect>

                <TabControl Background="Transparent" BorderThickness="0">

                    <TabItem Header="Ajuste de Ramos" Style="{StaticResource SubTabItemStyle}">
                        <ScrollViewer VerticalScrollBarVisibility="Auto" MaxHeight="480">
                            <StackPanel Margin="10">
                                <TextBlock Text="Precios de Ramos de Mayoreo" FontSize="18" FontWeight="Bold" Foreground="#CF8282" Margin="0,0,0,15"/>

                                <UniformGrid Columns="2" Margin="0,0,0,10">
                                    <StackPanel Margin="10,5">
                                        <TextBlock Text="Ramo 6 pz ($)" Foreground="#7F8C8D" Margin="0,0,0,4"/>
                                        <TextBox x:Name="txtPrecio6" Height="35" VerticalContentAlignment="Center" Padding="5,0" FontSize="14"/>
                                    </StackPanel>
                                    <StackPanel Margin="10,5">
                                        <TextBlock Text="Ramo 12 pz ($)" Foreground="#7F8C8D" Margin="0,0,0,4"/>
                                        <TextBox x:Name="txtPrecio12" Height="35" VerticalContentAlignment="Center" Padding="5,0" FontSize="14"/>
                                    </StackPanel>
                                    <StackPanel Margin="10,5">
                                        <TextBlock Text="Ramo 24 pz ($)" Foreground="#7F8C8D" Margin="0,0,0,4"/>
                                        <TextBox x:Name="txtPrecio24" Height="35" VerticalContentAlignment="Center" Padding="5,0" FontSize="14"/>
                                    </StackPanel>
                                    <StackPanel Margin="10,5">
                                        <TextBlock Text="Ramo 36 pz ($)" Foreground="#7F8C8D" Margin="0,0,0,4"/>
                                        <TextBox x:Name="txtPrecio36" Height="35" VerticalContentAlignment="Center" Padding="5,0" FontSize="14"/>
                                    </StackPanel>
                                    <StackPanel Margin="10,5">
                                        <TextBlock Text="Ramo 50 pz ($)" Foreground="#7F8C8D" Margin="0,0,0,4"/>
                                        <TextBox x:Name="txtPrecio50" Height="35" VerticalContentAlignment="Center" Padding="5,0" FontSize="14"/>
                                    </StackPanel>
                                    <StackPanel Margin="10,5">
                                        <TextBlock Text="Ramo 72 pz ($)" Foreground="#7F8C8D" Margin="0,0,0,4"/>
                                        <TextBox x:Name="txtPrecio72" Height="35" VerticalContentAlignment="Center" Padding="5,0" FontSize="14"/>
                                    </StackPanel>
                                    <StackPanel Margin="10,5">
                                        <TextBlock Text="Ramo 100 pz ($)" Foreground="#7F8C8D" Margin="0,0,0,4"/>
                                        <TextBox x:Name="txtPrecio100" Height="35" VerticalContentAlignment="Center" Padding="5,0" FontSize="14"/>
                                    </StackPanel>
                                    <StackPanel Margin="10,5">
                                        <TextBlock Text="Ramo 150 pz ($)" Foreground="#7F8C8D" Margin="0,0,0,4"/>
                                        <TextBox x:Name="txtPrecio150" Height="35" VerticalContentAlignment="Center" Padding="5,0" FontSize="14"/>
                                    </StackPanel>
                                    <StackPanel Margin="10,5">
                                        <TextBlock Text="Ramo 200 pz ($)" Foreground="#7F8C8D" Margin="0,0,0,4"/>
                                        <TextBox x:Name="txtPrecio200" Height="35" VerticalContentAlignment="Center" Padding="5,0" FontSize="14"/>
                                    </StackPanel>
                                    <StackPanel Margin="10,5">
                                        <TextBlock Text="Ramo 250 pz ($)" Foreground="#7F8C8D" Margin="0,0,0,4"/>
                                        <TextBox x:Name="txtPrecio250" Height="35" VerticalContentAlignment="Center" Padding="5,0" FontSize="14"/>
                                    </StackPanel>
                                </UniformGrid>

                                <Button Content="GUARDAR TODOS LOS PRECIOS" Click="btnAplicarAjustesRamos_Click" Height="48" Background="#27AE60" Foreground="White" FontWeight="Bold" Margin="10,15,10,10">
                                    <Button.Resources>
                                        <Style TargetType="Border">
                                            <Setter Property="CornerRadius" Value="8"/>
                                        </Style>
                                    </Button.Resources>
                                </Button>
                            </StackPanel>
                        </ScrollViewer>
                    </TabItem>

                    <TabItem Header="Costos por Flor" Style="{StaticResource SubTabItemStyle}">
                        <Grid Margin="10">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="320"/>
                            </Grid.ColumnDefinitions>
                            <StackPanel Grid.Column="0" Margin="0,0,20,0">
                                <DockPanel Margin="0,0,0,10">
                                    <TextBlock Text="Inventario de Flores" FontSize="18" FontWeight="Bold" Foreground="#CF8282"/>
                                    <TextBox x:Name="txtBuscarFlor" Width="180" HorizontalAlignment="Right" Height="25" TextChanged="txtBuscarFlor_TextChanged"/>
                                </DockPanel>
                                <DataGrid x:Name="dgPreciosFlores" AutoGenerateColumns="False" IsReadOnly="True" Height="400" SelectionChanged="dgPreciosFlores_SelectionChanged">
                                    <DataGrid.Columns>
                                        <DataGridTextColumn Header="Flor" Binding="{Binding Nombre}" Width="*"/>
                                        <DataGridTextColumn Header="Costo ($)" Binding="{Binding PrecioCompra, StringFormat=C2}" Width="100"/>
                                        <DataGridTextColumn Header="Venta ($)" Binding="{Binding PrecioVenta, StringFormat=C2}" Width="100"/>
                                    </DataGrid.Columns>
                                </DataGrid>
                            </StackPanel>
                            <Border Grid.Column="1" Background="#FDFEFE" CornerRadius="12" Padding="20">
                                <StackPanel>
                                    <TextBlock Text="Editar Flor" FontSize="20" FontWeight="Bold" Margin="0,0,0,15"/>
                                    <TextBlock Text="Margen (%):" Foreground="#CF8282"/>
                                    <TextBox x:Name="txtMargen" Text="50" Height="35" TextChanged="txtEditCosto_TextChanged"/>
                                    <TextBlock Text="Nombre:"/>
                                    <TextBox x:Name="txtEditNombre" IsReadOnly="True" Height="35"/>
                                    <TextBlock Text="Costo ($):"/>
                                    <TextBox x:Name="txtEditCosto" Height="35" TextChanged="txtEditCosto_TextChanged"/>
                                    <TextBlock Text="Precio Venta ($):"/>
                                    <TextBox x:Name="txtEditVenta" Height="35" Foreground="#27AE60" FontWeight="Bold"/>
                                    <Button x:Name="btnGuardarFlor" Content="ACTUALIZAR PRECIO" Click="btnAplicarAjustesFlores_Click" IsEnabled="False" Height="50" Background="#3498DB" Foreground="White" Margin="0,20"/>
                                </StackPanel>
                            </Border>
                        </Grid>
                    </TabItem>

                    <TabItem Header="Gestión de Usuarios" Style="{StaticResource SubTabItemStyle}">
                        <Grid Margin="15">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="*"/>
                            </Grid.RowDefinitions>

                            <TextBlock Text="Usuarios Pendientes de Activar" Grid.Row="0" FontSize="18" FontWeight="Bold" Foreground="#CF8282" Margin="0,0,0,10"/>
                            <DataGrid x:Name="dgUsuariosPendientes" Grid.Row="1" AutoGenerateColumns="False" CanUserAddRows="False" Background="White" Height="380">
                                <DataGrid.Columns>
                                    <DataGridTextColumn Header="Usuario" Binding="{Binding Username}" Width="*"/>

                                    <DataGridTemplateColumn Header="Acción" Width="150">
                                        <DataGridTemplateColumn.CellTemplate>
                                            <DataTemplate>
                                                <Button Content="ACTIVAR" Click="btnActivarUsuario_Click" Background="#27AE60" Foreground="White" Cursor="Hand"/>
                                            </DataTemplate>
                                        </DataGridTemplateColumn.CellTemplate>
                                    </DataGridTemplateColumn>

                                </DataGrid.Columns>
                            </DataGrid>
                        </Grid>
                    </TabItem>

                    <TabItem Header="Datos de Sucursal" Style="{StaticResource SubTabItemStyle}">
                        <Grid Margin="15">
                            <StackPanel>
                                <TextBlock Text="Identidad de la Sucursal Local y Cuentas" FontSize="18" FontWeight="Bold" Foreground="#CF8282" Margin="0,5,0,15"/>

                                <Border Background="#F8F9FA" Padding="20" CornerRadius="12" BorderThickness="1" BorderBrush="#E5E7E9">
                                    <Grid>
                                        <Grid.RowDefinitions>
                                            <RowDefinition Height="Auto"/>
                                            <RowDefinition Height="Auto"/>
                                        </Grid.RowDefinitions>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="380"/>
                                        </Grid.ColumnDefinitions>

                                        <StackPanel Grid.Row="0" Grid.Column="0" Margin="0,0,20,0" VerticalAlignment="Top">
                                            <TextBlock Text="Nombre Comercial o Identificador de esta Sucursal" Foreground="#7F8C8D" Margin="0,0,0,6" FontWeight="SemiBold" FontSize="13"/>
                                            <TextBox x:Name="txtNombreSucursalInput" Height="38" VerticalContentAlignment="Center" FontSize="14" Padding="8,0" Margin="0,0,0,15"/>

                                            <Button Content="GUARDAR CONFIGURACIÓN GENERAL" Click="btnGuardarSucursal_Click" Height="45" Background="#3498DB" Foreground="White" FontWeight="Bold" HorizontalAlignment="Left" Width="280" Cursor="Hand" Margin="0,15,0,0">
                                                <Button.Resources>
                                                    <Style TargetType="Border">
                                                        <Setter Property="CornerRadius" Value="6"/>
                                                    </Style>
                                                </Button.Resources>
                                            </Button>
                                        </StackPanel>

                                        <Border Grid.Row="0" Grid.Column="1" Grid.RowSpan="2" Background="White" BorderThickness="1" BorderBrush="#BDC3C7" CornerRadius="8" Padding="15">
                                            <StackPanel>
                                                <DockPanel Margin="0,0,0,8">
                                                    <TextBlock Text="Dueños de Cuenta Registrados" FontWeight="Bold" FontSize="13" Foreground="#2C3E50" VerticalAlignment="Center"/>
                                                    <TextBlock x:Name="txtContadorEncargados" Text="(0 personas)" FontWeight="SemiBold" FontSize="12" Foreground="#E74C3C" HorizontalAlignment="Right" VerticalAlignment="Center"/>
                                                </DockPanel>

                                                <ListBox x:Name="lbEncargadosCuentas" Height="150" Margin="0,0,0,10" Background="#FDFEFE" BorderThickness="1" BorderBrush="#D5DBDB"/>

                                                <Grid>
                                                    <Grid.ColumnDefinitions>
                                                        <ColumnDefinition Width="*"/>
                                                        <ColumnDefinition Width="80"/>
                                                    </Grid.ColumnDefinitions>
                                                    <TextBox x:Name="txtNuevoEncargadoInput" Grid.Column="0" Height="32" VerticalContentAlignment="Center" Padding="5,0"/>
                                                    <Button Content="Añadir" Click="btnAñadirEncargado_Click" Grid.Column="1" Height="32" Margin="5,0,0,0" Background="#27AE60" Foreground="White" FontWeight="Bold" Cursor="Hand"/>
                                                </Grid>

                                                <Button Content="Quitar Seleccionado" Click="btnQuitarEncargado_Click" Height="28" Margin="0,8,0,0" Background="#E74C3C" Foreground="White" FontSize="11" FontWeight="SemiBold" HorizontalAlignment="Right" Width="130" Cursor="Hand"/>
                                            </StackPanel>
                                        </Border>
                                    </Grid>
                                </Border>

                                <TextBlock Text="* Los nombres de los encargados alimentarán dinámicamente los selectores de transferencia en el módulo de ventas y balances." Foreground="#95A5A6" FontStyle="Italic" Margin="5,15,0,0" FontSize="12"/>
                            </StackPanel>
                        </Grid>
                    </TabItem>

                    <TabItem Header="Seguridad y Acceso" Style="{StaticResource SubTabItemStyle}">
                        <Grid Margin="15">
                            <StackPanel HorizontalAlignment="Left" Width="450">
                                <TextBlock Text="🔒 Modificar Credenciales Administrativas" FontSize="18" FontWeight="Bold" Foreground="#CF8282" Margin="0,5,0,5"/>
                                <TextBlock Text="Administra de forma segura el identificador de acceso y clave de este mostrador." Foreground="#7F8C8D" FontStyle="Italic" Margin="0,0,0,25"/>

                                <Border Background="#F8F9FA" Padding="30,40" CornerRadius="12" BorderThickness="1" BorderBrush="#E5E7E9">
                                    <StackPanel HorizontalAlignment="Center" Width="350">
                                        <TextBlock Text="Para modificar el nombre del usuario administrador junto con la contraseña, abre el panel seguro flotante." TextWrapping="Wrap" Foreground="#5D6D7E" FontSize="13" Margin="0,0,0,25" TextAlignment="Center"/>

                                        <Button Content="⚙️ ABRIR PANEL DE SEGURIDAD" Click="btnAbrirPanelSeguridad_Click" Height="48" Background="#27AE60" Foreground="White" FontWeight="Bold" Cursor="Hand">
                                            <Button.Resources>
                                                <Style TargetType="Border">
                                                    <Setter Property="CornerRadius" Value="6"/>
                                                </Style>
                                            </Button.Resources>
                                        </Button>
                                    </StackPanel>
                                </Border>
                            </StackPanel>
                        </Grid>
                    </TabItem>

                </TabControl>
            </Border>
        </StackPanel>
    </Grid>
</UserControl>