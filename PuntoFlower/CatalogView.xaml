<UserControl x:Class="PuntoFlower.Views.CatalogView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:PuntoFlower"
             mc:Ignorable="d" 
             d:DesignHeight="650" d:DesignWidth="900">

    <UserControl.Resources>
        <local:RutaAImagenConverter x:Key="RutaAImagenConverter"/>

        <Style x:Key="BaseCardBorderStyle" TargetType="Border">
            <Setter Property="Background" Value="White"/>
            <Setter Property="CornerRadius" Value="10"/>
            <Setter Property="BorderBrush" Value="#EBEDEF"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Margin" Value="10"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect BlurRadius="8" ShadowDepth="1" Color="#D5DBDB" Opacity="0.5"/>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Grid Background="#FDFEFE">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="#A93226" Padding="20,15">
            <TextBlock Text="Gestión de Catálogo Visual" FontSize="24" FontWeight="Bold" Foreground="White"/>
        </Border>

        <TabControl Grid.Row="1" Background="Transparent" BorderThickness="0" Margin="10">

            <TabItem Header="Flores e Insumos">
                <ScrollViewer VerticalScrollBarVisibility="Auto" Margin="0,10,0,0">
                    <ItemsControl x:Name="icProductos">
                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate>
                                <WrapPanel Orientation="Horizontal" HorizontalAlignment="Center"/>
                            </ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Border Style="{StaticResource BaseCardBorderStyle}" Width="180" Height="280">
                                    <StackPanel>
                                        <Border Height="140" Background="#F2F3F4" CornerRadius="10,10,0,0">
                                            <Image Source="{Binding RutaImagen, Converter={StaticResource RutaAImagenConverter}}" 
                                                   Stretch="Uniform" 
                                                   HorizontalAlignment="Center" 
                                                   VerticalAlignment="Center"/>
                                        </Border>

                                        <TextBlock Text="{Binding Nombre}" FontWeight="Bold" Margin="10,8,10,0" HorizontalAlignment="Center" TextTrimming="CharacterEllipsis" Foreground="#2C3E50"/>
                                        <TextBlock Text="{Binding PrecioVenta, StringFormat={}{0:C}}" Foreground="#27AE60" FontSize="15" FontWeight="SemiBold" HorizontalAlignment="Center" Margin="0,2,0,5"/>

                                        <DockPanel Margin="10,0,10,10">
                                            <Button Content="🗑" Click="btnEliminarFoto_Click" Tag="{Binding}" Width="35" Height="30" Background="#E74C3C" Foreground="White" FontWeight="Bold" Margin="0,0,5,0" ToolTip="Eliminar Foto" BorderThickness="0">
                                                <Button.Resources>
                                                    <Style TargetType="Border">
                                                        <Setter Property="CornerRadius" Value="5"/>
                                                    </Style>
                                                </Button.Resources>
                                            </Button>
                                            <Button Content="Cambiar Foto" Click="btnSubirFoto_Click" Tag="{Binding}" Height="30" Background="#A93226" Foreground="White" FontWeight="SemiBold" Cursor="Hand" BorderThickness="0">
                                                <Button.Resources>
                                                    <Style TargetType="Border">
                                                        <Setter Property="CornerRadius" Value="5"/>
                                                    </Style>
                                                </Button.Resources>
                                            </Button>
                                        </DockPanel>
                                    </StackPanel>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </ScrollViewer>
            </TabItem>

            <TabItem Header="Fotos de Ramos (Mayoreo)">
                <ScrollViewer VerticalScrollBarVisibility="Auto" Margin="0,10,0,0">
                    <WrapPanel x:Name="wpRamosMayoreo" HorizontalAlignment="Center">
                    </WrapPanel>
                </ScrollViewer>
            </TabItem>
        </TabControl>
    </Grid>
</UserControl>