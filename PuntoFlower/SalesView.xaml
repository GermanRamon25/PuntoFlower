<UserControl x:Class="PuntoFlower.Views.SalesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="1100">
    <Grid Background="#F5F5F5">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="380"/>
        </Grid.ColumnDefinitions>

        <ScrollViewer Grid.Column="0" VerticalScrollBarVisibility="Auto">
            <StackPanel Margin="20">
                <TextBlock Text="Punto de Venta" FontSize="28" FontWeight="Bold" Foreground="#2C3E50" Margin="0,0,0,20"/>

                <TextBlock Text="Ramos de Rosas (Selección Rápida)" FontSize="18" FontWeight="SemiBold" Margin="5,0,0,10" Foreground="#555"/>

                <WrapPanel Orientation="Horizontal">
                    <Button Width="190" Height="240" Margin="8" Background="White" Click="btnAgregarRamo_Click" BorderThickness="0">
                        <Button.Effect>
                            <DropShadowEffect BlurRadius="10" ShadowDepth="2" Color="#DDD"/>
                        </Button.Effect>
                        <StackPanel>
                            <Image Source="/Resources/Ramo.jpg" Height="150" Stretch="UniformToFill" Margin="0,5"/>
                            <TextBlock Text="Ramo 6 pz" HorizontalAlignment="Center" FontWeight="Bold" FontSize="16" Margin="0,5,0,0"/>
                            <TextBlock Text="$300.00" HorizontalAlignment="Center" Foreground="#E74C3C" FontSize="15"/>
                        </StackPanel>
                    </Button>

                    <Button Width="190" Height="240" Margin="8" Background="White" Click="btnAgregarRamo_Click" BorderThickness="0">
                        <Button.Effect>
                            <DropShadowEffect BlurRadius="10" ShadowDepth="2" Color="#DDD"/>
                        </Button.Effect>
                        <StackPanel>
                            <Image Source="/Resources/Ramo.jpg" Height="150" Stretch="UniformToFill" Margin="0,5"/>
                            <TextBlock Text="Ramo 12 pz" HorizontalAlignment="Center" FontWeight="Bold" FontSize="16" Margin="0,5,0,0"/>
                            <TextBlock Text="$450.00" HorizontalAlignment="Center" Foreground="#E74C3C" FontSize="15"/>
                        </StackPanel>
                    </Button>

                    <Button Width="190" Height="240" Margin="8" Background="White" Click="btnAgregarRamo_Click" BorderThickness="0">
                        <Button.Effect>
                            <DropShadowEffect BlurRadius="10" ShadowDepth="2" Color="#DDD"/>
                        </Button.Effect>
                        <StackPanel>
                            <Image Source="/Resources/Ramo.jpg" Height="150" Stretch="UniformToFill" Margin="0,5"/>
                            <TextBlock Text="Ramo 24 pz" HorizontalAlignment="Center" FontWeight="Bold" FontSize="16" Margin="0,5,0,0"/>
                            <TextBlock Text="$850.00" HorizontalAlignment="Center" Foreground="#E74C3C" FontSize="15"/>
                        </StackPanel>
                    </Button>

                    <Button Width="190" Height="240" Margin="8" Background="White" Click="btnAgregarRamo_Click" BorderThickness="0">
                        <Button.Effect>
                            <DropShadowEffect BlurRadius="10" ShadowDepth="2" Color="#DDD"/>
                        </Button.Effect>
                        <StackPanel>
                            <Image Source="/Resources/Ramo.jpg" Height="150" Stretch="UniformToFill" Margin="0,5"/>
                            <TextBlock Text="Ramo 50 pz" HorizontalAlignment="Center" FontWeight="Bold" FontSize="16" Margin="0,5,0,0"/>
                            <TextBlock Text="$1,650.00" HorizontalAlignment="Center" Foreground="#E74C3C" FontSize="15"/>
                        </StackPanel>
                    </Button>

                    <Button Width="190" Height="240" Margin="8" Background="White" Click="btnAgregarRamo_Click" BorderThickness="0">
                        <Button.Effect>
                            <DropShadowEffect BlurRadius="10" ShadowDepth="2" Color="#DDD"/>
                        </Button.Effect>
                        <StackPanel>
                            <Image Source="/Resources/Ramo.jpg" Height="150" Stretch="UniformToFill" Margin="0,5"/>
                            <TextBlock Text="Ramo 100 pz" HorizontalAlignment="Center" FontWeight="Bold" FontSize="16" Margin="0,5,0,0"/>
                            <TextBlock Text="$3,450.00" HorizontalAlignment="Center" Foreground="#E74C3C" FontSize="15"/>
                        </StackPanel>
                    </Button>
                </WrapPanel>

                <TextBlock Text="Otros Productos" FontSize="18" FontWeight="SemiBold" Margin="5,25,0,10" Foreground="#555"/>
                <UniformGrid Columns="3">
                    <Button Content="Corona Fúnebre" Height="90" Margin="5" Background="White" Foreground="#3498DB" FontWeight="Bold" Click="btnAgregarRamo_Click" BorderThickness="1" BorderBrush="#3498DB"/>
                    <Button Content="Medallón Floral" Height="90" Margin="5" Background="White" Foreground="#3498DB" FontWeight="Bold" Click="btnAgregarRamo_Click" BorderThickness="1" BorderBrush="#3498DB"/>
                    <Button Content="Arreglo Docena" Height="90" Margin="5" Background="White" Foreground="#3498DB" FontWeight="Bold" Click="btnAgregarRamo_Click" BorderThickness="1" BorderBrush="#3498DB"/>
                </UniformGrid>
            </StackPanel>
        </ScrollViewer>

        <Border Grid.Column="1" Background="White" Margin="10" CornerRadius="15">
            <Border.Effect>
                <DropShadowEffect BlurRadius="20" Color="#AAA" ShadowDepth="3"/>
            </Border.Effect>
            <Grid Margin="15">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <DockPanel Grid.Row="0" Margin="0,0,0,15">
                    <TextBlock Text="Resumen de Venta" FontSize="22" FontWeight="Bold"/>
                    <Button Content="Limpiar" DockPanel.Dock="Right" Background="Transparent" BorderThickness="0" Foreground="#E74C3C" Click="btnLimpiarTicket_Click"/>
                </DockPanel>

                <ListBox x:Name="lstVenta" Grid.Row="1" BorderThickness="0" Margin="0,0,0,10" HorizontalContentAlignment="Stretch">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <Border BorderThickness="0,0,0,1" BorderBrush="#EEE" Padding="0,8">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Text="{Binding ProductoNombre}" VerticalAlignment="Center" FontSize="14"/>
                                    <TextBlock Grid.Column="1" Text="{Binding Total, StringFormat={}{0:C}}" FontWeight="Bold" VerticalAlignment="Center" Margin="10,0"/>
                                    <Button Grid.Column="2" Content="✕" Click="btnEliminarItem_Click" Background="#FADBD8" Foreground="#E74C3C" Width="24" Height="24" BorderThickness="0" FontWeight="Bold"/>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>

                <Border Grid.Row="2" BorderThickness="0,2,0,0" BorderBrush="#F5F5F5" Padding="0,15,0,0">
                    <StackPanel>
                        <DockPanel Margin="0,0,0,15">
                            <TextBlock Text="TOTAL:" FontSize="26" FontWeight="Bold"/>
                            <TextBlock x:Name="txtTotal" Text="$0.00" FontSize="26" FontWeight="Bold" HorizontalAlignment="Right" Foreground="#27AE60"/>
                        </DockPanel>
                        <Button Content="CONFIRMAR VENTA" Height="60" Background="#27AE60" Foreground="White" FontSize="20" FontWeight="Bold" BorderThickness="0">
                            <Button.Resources>
                                <Style TargetType="Border">
                                    <Setter Property="CornerRadius" Value="10"/>
                                </Style>
                            </Button.Resources>
                        </Button>
                    </StackPanel>
                </Border>
            </Grid>
        </Border>
    </Grid>
</UserControl>