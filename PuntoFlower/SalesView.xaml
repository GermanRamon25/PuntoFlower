<UserControl x:Class="PuntoFlower.Views.SalesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" d:DesignHeight="650" d:DesignWidth="1100"
             Loaded="UserControl_Loaded">

    <UserControl.Resources>
        <Style TargetType="TabItem">
            <Setter Property="HeaderTemplate">
                <Setter.Value>
                    <DataTemplate>
                        <TextBlock x:Name="TabText" Text="{Binding}" Foreground="#5D6D7E" FontWeight="SemiBold" FontSize="13"/>
                        <DataTemplate.Triggers>
                            <DataTrigger Binding="{Binding IsSelected, RelativeSource={RelativeSource AncestorType=TabItem}}" Value="True">
                                <Setter TargetName="TabText" Property="Foreground" Value="#CF8282"/>
                                <Setter TargetName="TabText" Property="FontWeight" Value="Bold"/>
                            </DataTrigger>
                        </DataTemplate.Triggers>
                    </DataTemplate>
                </Setter.Value>
            </Setter>

            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TabItem">
                        <Border x:Name="Border" Margin="0,0,5,0" Padding="15,10" CornerRadius="10,10,0,0" Background="#E0E0E0" BorderThickness="0">
                            <ContentPresenter x:Name="ContentSite" VerticalAlignment="Center" HorizontalAlignment="Center" ContentSource="Header" RecognizesAccessKey="True"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="White"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="#FADBD8"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Grid Background="Transparent">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="380"/>
        </Grid.ColumnDefinitions>

        <TabControl Grid.Column="0" Margin="10" Background="Transparent" BorderThickness="0">
            <TabItem Header="Ramos de Mayoreo">
                <Border Background="White" CornerRadius="0,15,15,15">
                    <Border.Effect>
                        <DropShadowEffect BlurRadius="15" ShadowDepth="2" Color="#D5DBDB" Opacity="0.3"/>
                    </Border.Effect>
                    <ScrollViewer VerticalScrollBarVisibility="Auto">
                        <StackPanel Margin="15">
                            <TextBlock Text="Ramos de Rosas / Girasoles" FontSize="24" FontWeight="Bold" Foreground="#1E312F" Margin="0,0,0,15"/>

                            <GroupBox Header="1. Elegir Tamaño (Precios de Lista)" Margin="0,0,0,15">
                                <WrapPanel Orientation="Horizontal">
                                    <RadioButton x:Name="rbRamo6" Content="6 pz" Tag="6" Checked="Ramo_Checked" Margin="5" Style="{StaticResource {x:Type ToggleButton}}" Width="100" Height="40"/>
                                    <RadioButton x:Name="rbRamo12" Content="12 pz" Tag="12" Checked="Ramo_Checked" Margin="5" Style="{StaticResource {x:Type ToggleButton}}" Width="100" Height="40"/>
                                    <RadioButton x:Name="rbRamo24" Content="24 pz" Tag="24" Checked="Ramo_Checked" Margin="5" Style="{StaticResource {x:Type ToggleButton}}" Width="100" Height="40"/>
                                    <RadioButton x:Name="rbRamo50" Content="50 pz" Tag="50" Checked="Ramo_Checked" Margin="5" Style="{StaticResource {x:Type ToggleButton}}" Width="100" Height="40"/>
                                </WrapPanel>
                            </GroupBox>

                            <GroupBox Header="2. Composición del Ramo" Padding="10">
                                <StackPanel>
                                    <DockPanel Margin="0,0,0,10">
                                        <ComboBox x:Name="cbInsumosRamos" Width="220" DisplayMemberPath="Nombre" Margin="0,0,10,0" VerticalContentAlignment="Center"/>
                                        <TextBox x:Name="txtCantFlorRamo" Width="60" VerticalContentAlignment="Center" Text="0" Margin="0,0,10,0" HorizontalContentAlignment="Center"/>
                                        <Button Content="Añadir" Click="btnAgregarAlRamo_Click" Background="#3498DB" Foreground="White" FontWeight="Bold" Width="100" Height="30"/>
                                    </DockPanel>
                                    <TextBlock x:Name="lblProgresoRamo" Text="Seleccionadas: 0 / 0" FontSize="14" FontWeight="Bold" Foreground="#E67E22" Margin="0,5"/>
                                    <Button Content="Finalizar y Agregar al Ticket" Click="btnFinalizarRamo_Click" Background="#27AE60" Foreground="White" Height="45" Margin="0,15,0,15" FontWeight="Bold"/>
                                    <Border Background="#ECF0F1" CornerRadius="8" BorderThickness="1" BorderBrush="#BDC3C7" Height="200" HorizontalAlignment="Stretch">
                                        <Grid>
                                            <TextBlock x:Name="txtPlaceholder" Text="Sin foto de referencia" VerticalAlignment="Center" HorizontalAlignment="Center" Foreground="#95A5A6" FontStyle="Italic"/>
                                            <Image x:Name="imgReferencia" Stretch="Uniform" Margin="10"/>
                                        </Grid>
                                    </Border>
                                </StackPanel>
                            </GroupBox>
                        </StackPanel>
                    </ScrollViewer>
                </Border>
            </TabItem>

            <TabItem Header="Venta Libre / Insumos">
                <Border Background="White" CornerRadius="0,15,15,15">
                    <Border.Effect>
                        <DropShadowEffect BlurRadius="15" ShadowDepth="2" Color="#D5DBDB" Opacity="0.3"/>
                    </Border.Effect>
                    <StackPanel Margin="15">
                        <TextBlock Text="Flor Suelta, Bases y Accesorios" FontSize="24" FontWeight="Bold" Foreground="#1E312F" Margin="0,0,0,15"/>
                        <Border Background="White" Padding="20" CornerRadius="10">
                            <StackPanel>
                                <TextBlock Text="Seleccionar Producto" Foreground="#7F8C8D" Margin="0,0,0,5"/>
                                <ComboBox x:Name="cbInsumosLibre" Height="40" DisplayMemberPath="Nombre" Margin="0,5,0,20"/>
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>
                                    <StackPanel Grid.Column="0" Margin="0,0,10,0">
                                        <TextBlock Text="Cantidad" Foreground="#7F8C8D"/>
                                        <TextBox x:Name="txtCantLibre" Height="40" VerticalContentAlignment="Center" Text="1" FontSize="16" HorizontalContentAlignment="Center"/>
                                    </StackPanel>
                                    <Button Grid.Column="1" Content="Agregar al Ticket" Click="btnAgregarVentaLibre_Click" Background="#27AE60" Foreground="White" FontWeight="Bold" Height="40" VerticalAlignment="Bottom"/>
                                </Grid>
                            </StackPanel>
                        </Border>
                    </StackPanel>
                </Border>
            </TabItem>

            <TabItem Header="Arreglos Especiales">
                <Border Background="White" CornerRadius="0,15,15,15">
                    <Border.Effect>
                        <DropShadowEffect BlurRadius="15" ShadowDepth="2" Color="#D5DBDB" Opacity="0.3"/>
                    </Border.Effect>
                    <StackPanel Margin="15">
                        <TextBlock Text="Coronas, Novias y Fúnebres" FontSize="24" FontWeight="Bold" Foreground="#1E312F" Margin="0,0,0,15"/>
                        <Border Background="White" Padding="20" CornerRadius="10">
                            <StackPanel>
                                <TextBlock Text="Nombre del Servicio / Arreglo" Foreground="#7F8C8D"/>
                                <TextBox x:Name="txtNombreEspecial" Height="40" Margin="0,5,0,15" VerticalContentAlignment="Center"/>
                                <TextBlock Text="Precio Acordado ($)" Foreground="#7F8C8D"/>
                                <TextBox x:Name="txtPrecioEspecial" Height="40" Margin="0,5,0,20" VerticalContentAlignment="Center" FontSize="18" FontWeight="Bold"/>
                                <Button Content="Agregar Pedido Especial" Click="btnAgregarEspecial_Click" Background="#27AE60" Foreground="White" FontWeight="Bold" Height="45"/>
                            </StackPanel>
                        </Border>
                    </StackPanel>
                </Border>
            </TabItem>
        </TabControl>

        <Border Grid.Column="1" Background="White" Margin="10" CornerRadius="15">
            <Border.Effect>
                <DropShadowEffect BlurRadius="15" ShadowDepth="2" Color="#D5DBDB" Opacity="0.4"/>
            </Border.Effect>
            <Grid Margin="15">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <DockPanel Grid.Row="0" Margin="0,0,0,15">
                    <TextBlock Text="Resumen de Venta" FontSize="20" FontWeight="Bold" Foreground="#1E312F"/>
                    <Button Content="Vaciar" Click="btnLimpiarTicket_Click" DockPanel.Dock="Right" Background="Transparent" BorderThickness="0" Foreground="#E74C3C" FontWeight="Bold"/>
                </DockPanel>
                <ListBox x:Name="lstVenta" Grid.Row="1" BorderThickness="0" HorizontalContentAlignment="Stretch">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <Border BorderThickness="0,0,0,1" BorderBrush="#EEE" Padding="5">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    <StackPanel Grid.Column="0">
                                        <TextBlock Text="{Binding ProductoNombre}" FontWeight="Bold" TextWrapping="Wrap" Foreground="#2C3E50"/>
                                        <TextBlock Text="{Binding DetalleVisual}" FontSize="10" Foreground="#95A5A6"/>
                                    </StackPanel>
                                    <TextBlock Grid.Column="1" Text="{Binding Total, StringFormat={}{0:C}}" VerticalAlignment="Center" Margin="10,0" FontWeight="Bold" Foreground="#27AE60"/>
                                    <Button Grid.Column="2" Content="✕" Click="btnEliminarItem_Click" Background="#FADBD8" Foreground="#E74C3C" Width="24" Height="24" BorderThickness="0" FontWeight="Bold"/>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
                <StackPanel Grid.Row="2" Margin="0,15,0,0">
                    <TextBlock x:Name="txtTotal" Text="Total: $0.00" FontSize="28" FontWeight="Bold" Foreground="#27AE60" HorizontalAlignment="Right"/>
                    <UniformGrid Columns="2" Margin="0,10">
                        <StackPanel Margin="0,0,5,0">
                            <TextBlock Text="Recibido ($)" Foreground="#7F8C8D" FontSize="12"/>
                            <TextBox x:Name="txtPagoCon" Height="35" FontSize="16" FontWeight="Bold" VerticalContentAlignment="Center" TextChanged="txtPagoCon_TextChanged"/>
                        </StackPanel>
                        <StackPanel Margin="5,0,0,0">
                            <TextBlock Text="Cambio ($)" Foreground="#7F8C8D" FontSize="12"/>
                            <TextBlock x:Name="txtCambio" Text="$0.00" Height="35" FontSize="16" FontWeight="Bold" Foreground="#E74C3C" Padding="0,5,0,0"/>
                        </StackPanel>
                    </UniformGrid>
                    <Button Content="CONFIRMAR COBRO" Click="btnConfirmarVenta_Click" Height="60" Background="#27AE60" Foreground="White" FontWeight="Bold" Margin="0,10,0,0" BorderThickness="0"/>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</UserControl>