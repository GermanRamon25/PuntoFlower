<UserControl x:Class="PuntoFlower.Views.SalesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" d:DesignHeight="650" d:DesignWidth="1100"
             Loaded="UserControl_Loaded">

    <Grid Background="#F5F5F5">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="380"/>
        </Grid.ColumnDefinitions>

        <ScrollViewer Grid.Column="0" VerticalScrollBarVisibility="Auto">
            <StackPanel Margin="20">
                <TextBlock Text="Punto de Venta - Ramos Personalizados" FontSize="28" FontWeight="Bold" Foreground="#2C3E50" Margin="0,0,0,20"/>

                <GroupBox Header="1. Tamaño del Ramo (Precio Mayoreo)" Margin="0,0,0,15" Padding="10">
                    <WrapPanel Orientation="Horizontal">
                        <RadioButton Content="6 pz ($300)" Tag="6" Checked="Ramo_Checked" Margin="5" Style="{StaticResource {x:Type ToggleButton}}" Width="120" Height="40"/>
                        <RadioButton Content="12 pz ($450)" Tag="12" Checked="Ramo_Checked" Margin="5" Style="{StaticResource {x:Type ToggleButton}}" Width="120" Height="40"/>
                        <RadioButton Content="18 pz ($650)" Tag="18" Checked="Ramo_Checked" Margin="5" Style="{StaticResource {x:Type ToggleButton}}" Width="120" Height="40"/>
                        <RadioButton Content="24 pz ($850)" Tag="24" Checked="Ramo_Checked" Margin="5" Style="{StaticResource {x:Type ToggleButton}}" Width="120" Height="40"/>
                        <RadioButton Content="36 pz ($1,200)" Tag="36" Checked="Ramo_Checked" Margin="5" Style="{StaticResource {x:Type ToggleButton}}" Width="120" Height="40"/>
                        <RadioButton Content="50 pz ($1,650)" Tag="50" Checked="Ramo_Checked" Margin="5" Style="{StaticResource {x:Type ToggleButton}}" Width="120" Height="40"/>
                    </WrapPanel>
                </GroupBox>

                <GroupBox Header="2. Composición del Ramo" Margin="0,0,0,15" Padding="10">
                    <StackPanel>
                        <DockPanel Margin="0,0,0,10">
                            <StackPanel Margin="0,0,10,0">
                                <TextBlock Text="Tipo de Flor" FontWeight="SemiBold"/>
                                <ComboBox x:Name="cbInsumos" Width="200" Height="35" DisplayMemberPath="Nombre"/>
                            </StackPanel>
                            <StackPanel Margin="0,0,10,0">
                                <TextBlock Text="Cantidad" FontWeight="SemiBold"/>
                                <TextBox x:Name="txtCantFlor" Width="80" Height="35" VerticalContentAlignment="Center" Text="0"/>
                            </StackPanel>
                            <Button Content="Agregar al Ramo" Click="btnAgregarAlRamo_Click" Width="150" Height="35" VerticalAlignment="Bottom" Background="#3498DB" Foreground="White" FontWeight="Bold"/>
                        </DockPanel>

                        <TextBlock x:Name="lblProgresoRamo" Text="Flores seleccionadas: 0 / 0" FontSize="16" Foreground="#E67E22" FontWeight="Bold"/>
                    </StackPanel>
                </GroupBox>

                <Button Content="Finalizar Ramo y Agregar al Ticket" Click="btnFinalizarRamo_Click" Height="50" Background="#27AE60" Foreground="White" FontSize="18" FontWeight="Bold" Margin="0,10,0,0"/>
            </StackPanel>
        </ScrollViewer>

        <Border Grid.Column="1" Background="White" Margin="10" CornerRadius="15">
            <Grid Margin="15">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <TextBlock Text="Resumen del Ticket" FontSize="22" FontWeight="Bold" Margin="0,0,0,15"/>

                <ListBox x:Name="lstVenta" Grid.Row="1" BorderThickness="0">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <Border BorderThickness="0,0,0,1" BorderBrush="#EEE" Padding="5">
                                <StackPanel>
                                    <DockPanel>
                                        <TextBlock Text="{Binding ProductoNombre}" FontWeight="Bold"/>
                                        <TextBlock Text="{Binding Total, StringFormat={}{0:C}}" DockPanel.Dock="Right"/>
                                    </DockPanel>
                                    <TextBlock Text="Contenido detallado en sistema" FontSize="11" Foreground="#7F8C8D"/>
                                </StackPanel>
                            </Border>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>

                <StackPanel Grid.Row="2" Margin="0,15,0,0">
                    <TextBlock x:Name="txtTotal" Text="Total: $0.00" FontSize="26" FontWeight="Bold" Foreground="#27AE60" HorizontalAlignment="Right"/>
                    <Button Content="CONFIRMAR VENTA TOTAL" Click="btnConfirmarVenta_Click" Height="60" Background="#27AE60" Foreground="White" FontWeight="Bold" Margin="0,10,0,0"/>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</UserControl>