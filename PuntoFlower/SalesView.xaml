<UserControl x:Class="PuntoFlower.Views.SalesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" d:DesignHeight="650" d:DesignWidth="1100"
             Loaded="UserControl_Loaded">

    <Grid Background="#F5F5F5">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="380"/>
        </Grid.ColumnDefinitions>

        <TabControl Grid.Column="0" Margin="10" Background="Transparent" BorderThickness="0">
            <TabItem Header="Ramos de Mayoreo">
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <StackPanel Margin="15">
                        <TextBlock Text="Configurador de Ramos" FontSize="24" FontWeight="Bold" Foreground="#2C3E50" Margin="0,0,0,15"/>

                        <GroupBox Header="1. Elegir Tamaño" Margin="0,0,0,15">
                            <WrapPanel Orientation="Horizontal">
                                <RadioButton Content="6 pz ($300)" Tag="6" Checked="Ramo_Checked" Margin="5" Style="{StaticResource {x:Type ToggleButton}}" Width="100" Height="40"/>
                                <RadioButton Content="12 pz ($450)" Tag="12" Checked="Ramo_Checked" Margin="5" Style="{StaticResource {x:Type ToggleButton}}" Width="100" Height="40"/>
                                <RadioButton Content="18 pz ($650)" Tag="18" Checked="Ramo_Checked" Margin="5" Style="{StaticResource {x:Type ToggleButton}}" Width="100" Height="40"/>
                                <RadioButton Content="24 pz ($850)" Tag="24" Checked="Ramo_Checked" Margin="5" Style="{StaticResource {x:Type ToggleButton}}" Width="100" Height="40"/>
                            </WrapPanel>
                        </GroupBox>

                        <GroupBox Header="2. Composición" Padding="10">
                            <StackPanel>
                                <DockPanel Margin="0,0,0,10">
                                    <ComboBox x:Name="cbInsumosRamos" Width="180" DisplayMemberPath="Nombre" Margin="0,0,10,0"/>
                                    <TextBox x:Name="txtCantFlorRamo" Width="60" VerticalContentAlignment="Center" Text="0" Margin="0,0,10,0"/>
                                    <Button Content="Añadir" Click="btnAgregarAlRamo_Click" Background="#3498DB" Foreground="White" FontWeight="Bold" Width="100"/>
                                </DockPanel>
                                <TextBlock x:Name="lblProgresoRamo" Text="Progreso: 0 / 0" FontSize="14" FontWeight="Bold" Foreground="#E67E22"/>
                                <Button Content="Finalizar Ramo" Click="btnFinalizarRamo_Click" Background="#27AE60" Foreground="White" Height="40" Margin="0,15,0,0"/>
                            </StackPanel>
                        </GroupBox>
                    </StackPanel>
                </ScrollViewer>
            </TabItem>

            <TabItem Header="Venta Libre (Insumos)">
                <StackPanel Margin="15">
                    <TextBlock Text="Venta Individual / Insumos" FontSize="24" FontWeight="Bold" Foreground="#2C3E50" Margin="0,0,0,15"/>
                    <Border Background="White" Padding="15" CornerRadius="8">
                        <StackPanel>
                            <TextBlock Text="Seleccionar Artículo (Usa precio de Inventario)" Margin="0,0,0,5" Foreground="#7F8C8D"/>
                            <ComboBox x:Name="cbInsumosLibre" Height="35" DisplayMemberPath="Nombre" Margin="0,0,0,15"/>

                            <UniformGrid Columns="2">
                                <StackPanel Margin="0,0,10,0">
                                    <TextBlock Text="Cantidad" Foreground="#7F8C8D"/>
                                    <TextBox x:Name="txtCantLibre" Height="35" VerticalContentAlignment="Center" Text="1"/>
                                </StackPanel>
                                <Button Content="Agregar al Ticket" Click="btnAgregarVentaLibre_Click" Background="#3498DB" Foreground="White" FontWeight="Bold" VerticalAlignment="Bottom" Height="35"/>
                            </UniformGrid>
                        </StackPanel>
                    </Border>
                </StackPanel>
            </TabItem>
        </TabControl>

        <Border Grid.Column="1" Background="White" Margin="10" CornerRadius="15">
            <Grid Margin="15">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <DockPanel Grid.Row="0" Margin="0,0,0,15">
                    <TextBlock Text="Ticket Actual" FontSize="20" FontWeight="Bold"/>
                    <Button Content="Limpiar" Click="btnLimpiarTicket_Click" DockPanel.Dock="Right" Background="Transparent" BorderThickness="0" Foreground="#E74C3C"/>
                </DockPanel>

                <ListBox x:Name="lstVenta" Grid.Row="1" BorderThickness="0" HorizontalContentAlignment="Stretch">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <Border BorderThickness="0,0,0,1" BorderBrush="#EEE" Padding="5">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    <StackPanel Grid.Column="0">
                                        <TextBlock Text="{Binding ProductoNombre}" FontWeight="Bold" TextWrapping="Wrap"/>
                                        <TextBlock Text="{Binding DetalleVisual}" FontSize="10" Foreground="#95A5A6"/>
                                    </StackPanel>
                                    <TextBlock Grid.Column="1" Text="{Binding Total, StringFormat={}{0:C}}" VerticalAlignment="Center" Margin="10,0"/>
                                    <Button Grid.Column="2" Content="✕" Click="btnEliminarItem_Click" Background="#FADBD8" Foreground="#E74C3C" Width="24" Height="24" BorderThickness="0" FontWeight="Bold"/>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>

                <StackPanel Grid.Row="2" Margin="0,15,0,0">
                    <TextBlock x:Name="txtTotal" Text="Total: $0.00" FontSize="26" FontWeight="Bold" Foreground="#27AE60" HorizontalAlignment="Right"/>
                    <Button Content="CONFIRMAR COBRO" Click="btnConfirmarVenta_Click" Height="60" Background="#27AE60" Foreground="White" FontWeight="Bold" Margin="0,10,0,0" BorderThickness="0"/>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</UserControl>