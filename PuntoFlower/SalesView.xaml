<UserControl x:Class="PuntoFlower.Views.SalesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" d:DesignHeight="650" d:DesignWidth="1100"
             Loaded="UserControl_Loaded">

    <Grid Background="#F5F5F5">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="380"/>
        </Grid.ColumnDefinitions>

        <TabControl Grid.Column="0" Margin="10" Background="Transparent" BorderThickness="0">
            <TabItem Header="Ramos de Mayoreo">
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <StackPanel Margin="15">
                        <TextBlock Text="Ramos de Rosas / Girasoles" FontSize="24" FontWeight="Bold" Foreground="#2C3E50" Margin="0,0,0,15"/>
                        <GroupBox Header="1. Elegir Tamaño (Precios de Lista)" Margin="0,0,0,15">
                            <WrapPanel Orientation="Horizontal">
                                <RadioButton Content="6 pz ($300)" Tag="6" Checked="Ramo_Checked" Margin="5" Style="{StaticResource {x:Type ToggleButton}}" Width="100" Height="40"/>
                                <RadioButton Content="12 pz ($450)" Tag="12" Checked="Ramo_Checked" Margin="5" Style="{StaticResource {x:Type ToggleButton}}" Width="100" Height="40"/>
                                <RadioButton Content="24 pz ($850)" Tag="24" Checked="Ramo_Checked" Margin="5" Style="{StaticResource {x:Type ToggleButton}}" Width="100" Height="40"/>
                                <RadioButton Content="50 pz ($1,650)" Tag="50" Checked="Ramo_Checked" Margin="5" Style="{StaticResource {x:Type ToggleButton}}" Width="100" Height="40"/>
                            </WrapPanel>
                        </GroupBox>

                        <GroupBox Header="2. Composición del Ramo" Padding="10">
                            <StackPanel>
                                <DockPanel Margin="0,0,0,10">
                                    <ComboBox x:Name="cbInsumosRamos" Width="180" DisplayMemberPath="Nombre" Margin="0,0,10,0"/>
                                    <TextBox x:Name="txtCantFlorRamo" Width="60" VerticalContentAlignment="Center" Text="0" Margin="0,0,10,0"/>
                                    <Button Content="Añadir" Click="btnAgregarAlRamo_Click" Background="#3498DB" Foreground="White" FontWeight="Bold" Width="100"/>
                                </DockPanel>
                                <TextBlock x:Name="lblProgresoRamo" Text="Progreso: 0 / 0" FontSize="14" FontWeight="Bold" Foreground="#E67E22"/>
                                <Button Content="Finalizar y Agregar al Ticket" Click="btnFinalizarRamo_Click" Background="#27AE60" Foreground="White" Height="45" Margin="0,15,0,0"/>
                            </StackPanel>
                        </GroupBox>
                    </StackPanel>
                </ScrollViewer>
            </TabItem>

            <TabItem Header="Venta Libre / Insumos">
                <StackPanel Margin="15">
                    <TextBlock Text="Flor Suelta, Bases y Accesorios" FontSize="24" FontWeight="Bold" Foreground="#2C3E50" Margin="0,0,0,15"/>
                    <Border Background="White" Padding="20" CornerRadius="10">
                        <StackPanel>
                            <TextBlock Text="Seleccionar Producto" Foreground="#7F8C8D"/>
                            <ComboBox x:Name="cbInsumosLibre" Height="40" DisplayMemberPath="Nombre" Margin="0,5,0,20"/>

                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <StackPanel Grid.Column="0" Margin="0,0,10,0">
                                    <TextBlock Text="Cantidad" Foreground="#7F8C8D"/>
                                    <TextBox x:Name="txtCantLibre" Height="40" VerticalContentAlignment="Center" Text="1" FontSize="16"/>
                                </StackPanel>
                                <Button Grid.Column="1" Content="Agregar al Ticket" Click="btnAgregarVentaLibre_Click" Background="#3498DB" Foreground="White" FontWeight="Bold" Height="40" VerticalAlignment="Bottom"/>
                            </Grid>
                            <TextBlock Text="*Usa el precio registrado en inventario (Ej: Mariposa $10, Oasis $60)" FontSize="11" Foreground="#95A5A6" Margin="0,15,0,0" FontStyle="Italic"/>
                        </StackPanel>
                    </Border>
                </StackPanel>
            </TabItem>

            <TabItem Header="Arreglos Especiales">
                <StackPanel Margin="15">
                    <TextBlock Text="Coronas, Novias y Fúnebres" FontSize="24" FontWeight="Bold" Foreground="#2C3E50" Margin="0,0,0,15"/>
                    <Border Background="White" Padding="20" CornerRadius="10">
                        <StackPanel>
                            <TextBlock Text="Nombre del Servicio / Arreglo (Ej: Corona Fúnebre Yumbo)" Foreground="#7F8C8D"/>
                            <TextBox x:Name="txtNombreEspecial" Height="40" Margin="0,5,0,15" VerticalContentAlignment="Center"/>

                            <TextBlock Text="Precio Acordado ($)" Foreground="#7F8C8D"/>
                            <TextBox x:Name="txtPrecioEspecial" Height="40" Margin="0,5,0,20" VerticalContentAlignment="Center" FontSize="18" FontWeight="Bold"/>

                            <Button Content="Agregar Pedido Especial" Click="btnAgregarEspecial_Click" Background="#8E44AD" Foreground="White" FontWeight="Bold" Height="45"/>
                        </StackPanel>
                    </Border>
                </StackPanel>
            </TabItem>
        </TabControl>

        <Border Grid.Column="1" Background="White" Margin="10" CornerRadius="15">
            <Grid Margin="15">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <DockPanel Grid.Row="0" Margin="0,0,0,15">
                    <TextBlock Text="Resumen de Venta" FontSize="20" FontWeight="Bold"/>
                    <Button Content="Vaciar" Click="btnLimpiarTicket_Click" DockPanel.Dock="Right" Background="Transparent" BorderThickness="0" Foreground="#E74C3C"/>
                </DockPanel>

                <ListBox x:Name="lstVenta" Grid.Row="1" BorderThickness="0" HorizontalContentAlignment="Stretch">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <Border BorderThickness="0,0,0,1" BorderBrush="#EEE" Padding="5">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    <StackPanel Grid.Column="0">
                                        <TextBlock Text="{Binding ProductoNombre}" FontWeight="Bold" TextWrapping="Wrap"/>
                                        <TextBlock Text="{Binding DetalleVisual}" FontSize="10" Foreground="#95A5A6"/>
                                    </StackPanel>
                                    <TextBlock Grid.Column="1" Text="{Binding Total, StringFormat={}{0:C}}" VerticalAlignment="Center" Margin="10,0" FontWeight="SemiBold"/>
                                    <Button Grid.Column="2" Content="✕" Click="btnEliminarItem_Click" Background="#FADBD8" Foreground="#E74C3C" Width="24" Height="24" BorderThickness="0" FontWeight="Bold"/>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>

                <StackPanel Grid.Row="2" Margin="0,15,0,0">
                    <TextBlock x:Name="txtTotal" Text="Total: $0.00" FontSize="28" FontWeight="Bold" Foreground="#27AE60" HorizontalAlignment="Right"/>
                    <Button Content="CONFIRMAR COBRO" Click="btnConfirmarVenta_Click" Height="60" Background="#27AE60" Foreground="White" FontWeight="Bold" Margin="0,10,0,0" BorderThickness="0"/>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</UserControl>