<UserControl x:Class="PuntoFlower.Views.DashboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800">
    <Grid Background="#F5F5F5">
        <ScrollViewer VerticalScrollBarVisibility="Auto">
            <StackPanel Margin="20">
                <TextBlock Text="Resumen General" FontSize="26" FontWeight="Bold" Foreground="#2C3E50" Margin="0,0,0,20"/>

                <UniformGrid Columns="4" Height="120">
                    <Border Background="White" CornerRadius="10" Margin="5" Padding="15">
                        <Border.Effect>
                            <DropShadowEffect BlurRadius="10" ShadowDepth="1" Color="#DDD"/>
                        </Border.Effect>
                        <StackPanel VerticalAlignment="Center">
                            <TextBlock Text="Ventas del Mes" Foreground="#7F8C8D" FontSize="14"/>
                            <TextBlock x:Name="txtVentasMes" Text="$0.00" Foreground="#27AE60" FontSize="22" FontWeight="Bold" Margin="0,5"/>
                        </StackPanel>
                    </Border>

                    <Border Background="White" CornerRadius="10" Margin="5" Padding="15">
                        <Border.Effect>
                            <DropShadowEffect BlurRadius="10" ShadowDepth="1" Color="#DDD"/>
                        </Border.Effect>
                        <StackPanel VerticalAlignment="Center">
                            <TextBlock Text="Inversión Stock" Foreground="#7F8C8D" FontSize="14"/>
                            <TextBlock x:Name="txtGastosSurtido" Text="$0.00" Foreground="#E74C3C" FontSize="22" FontWeight="Bold" Margin="0,5"/>
                        </StackPanel>
                    </Border>

                    <Border Background="White" CornerRadius="10" Margin="5" Padding="15">
                        <Border.Effect>
                            <DropShadowEffect BlurRadius="10" ShadowDepth="1" Color="#DDD"/>
                        </Border.Effect>
                        <StackPanel VerticalAlignment="Center">
                            <TextBlock Text="Stock Crítico" Foreground="#7F8C8D" FontSize="14"/>
                            <TextBlock x:Name="txtStockAlerta" Text="0 Flores" Foreground="#F39C12" FontSize="22" FontWeight="Bold" Margin="0,5"/>
                        </StackPanel>
                    </Border>

                    <Border Background="White" CornerRadius="10" Margin="5" Padding="15">
                        <Border.Effect>
                            <DropShadowEffect BlurRadius="10" ShadowDepth="1" Color="#DDD"/>
                        </Border.Effect>
                        <StackPanel VerticalAlignment="Center">
                            <TextBlock Text="Pedidos Hoy" Foreground="#7F8C8D" FontSize="14"/>
                            <TextBlock x:Name="txtPedidosHoy" Text="0" Foreground="#3498DB" FontSize="22" FontWeight="Bold" Margin="0,5"/>
                        </StackPanel>
                    </Border>
                </UniformGrid>

                <Border Background="White" CornerRadius="10" Margin="5,20,5,20">
                    <Border.Effect>
                        <DropShadowEffect BlurRadius="10" ShadowDepth="1" Color="#DDD"/>
                    </Border.Effect>
                    <StackPanel Margin="20">
                        <DockPanel LastChildFill="False" Margin="0,0,0,15">
                            <TextBlock Text="Próximas Entregas (Hoy)" FontSize="18" FontWeight="SemiBold" Foreground="#2C3E50"/>
                            <TextBlock x:Name="lblContadorHoy" Text="0 pendientes" DockPanel.Dock="Right" VerticalAlignment="Center" Foreground="#7F8C8D"/>
                        </DockPanel>

                        <Separator Background="#EEE" Margin="0,0,0,10"/>

                        <TextBlock x:Name="lblMensajeAgenda" Text="No hay entregas pendientes para hoy" 
                                   Foreground="#BBB" HorizontalAlignment="Center" Margin="30" Visibility="Collapsed"/>

                        <ItemsControl x:Name="icEntregasHoy">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Border Background="#F8F9FA" Margin="0,0,0,8" Padding="12" CornerRadius="8" BorderThickness="4,0,0,0" BorderBrush="{Binding ColorEstado}">
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="85"/>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="Auto"/>
                                            </Grid.ColumnDefinitions>

                                            <StackPanel VerticalAlignment="Center">
                                                <TextBlock Text="{Binding FechaEntrega, StringFormat=t}" FontWeight="Bold" FontSize="16" Foreground="#2C3E50"/>
                                                <TextBlock Text="Hora" FontSize="10" Foreground="#95A5A6"/>
                                            </StackPanel>

                                            <StackPanel Grid.Column="1" Margin="15,0" VerticalAlignment="Center">
                                                <TextBlock Text="{Binding ClienteNombre}" FontWeight="Bold" FontSize="15" Foreground="#2C3E50"/>
                                                <TextBlock Text="{Binding Descripcion}" Foreground="#5D6D7E" FontSize="12" FontStyle="Italic"/>
                                            </StackPanel>

                                            <Border Grid.Column="2" Background="White" CornerRadius="5" Padding="10,5" VerticalAlignment="Center" BorderThickness="1" BorderBrush="#DDD">
                                                <TextBlock Text="{Binding Estado}" FontWeight="Bold" FontSize="10" Foreground="{Binding ColorEstado}"/>
                                            </Border>
                                        </Grid>
                                    </Border>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </StackPanel>
                </Border>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</UserControl>