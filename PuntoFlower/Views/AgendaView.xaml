<UserControl x:Class="PuntoFlower.Views.AgendaView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:PuntoFlower.Views"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="1000">
    <Grid Background="#F5F5F5">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="White" Margin="10" CornerRadius="8" Padding="15">
            <Border.Effect>
                <DropShadowEffect BlurRadius="10" ShadowDepth="1" Color="#DDD"/>
            </Border.Effect>
            <DockPanel LastChildFill="False">
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="📅 Agenda de Pedidos" FontSize="24" FontWeight="Bold" VerticalAlignment="Center" Margin="0,0,20,0"/>
                    <Button x:Name="btnFiltrarTodos" Content="Todos" Click="btnFiltrarTodos_Click" Width="80" Margin="5,0" Background="#3498DB" Foreground="White" BorderThickness="0" Cursor="Hand"/>
                    <Button x:Name="btnFiltrarHoy" Content="Para Hoy" Click="btnFiltrarHoy_Click" Width="80" Margin="5,0" Background="White" BorderBrush="#DDD" Cursor="Hand"/>
                    <Button x:Name="btnFiltrarCerrados" Content="Ver Liquidados" Click="btnFiltrarCerrados_Click" Width="110" Margin="5,0" Background="White" BorderBrush="#DDD" Cursor="Hand"/>
                </StackPanel>

                <Button Content="+ Agendar Nuevo Pedido" DockPanel.Dock="Right" 
                        Click="btnNuevoPedido_Click" Width="180" Height="35" 
                        Background="#27AE60" Foreground="White" FontWeight="Bold" BorderThickness="0" Cursor="Hand"/>
            </DockPanel>
        </Border>

        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <ItemsControl x:Name="icPedidos" Margin="10">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <WrapPanel Orientation="Horizontal"/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>

                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border x:Name="CardBorder" Width="310" Background="White" Margin="10" CornerRadius="12" Padding="15" BorderThickness="4,0,0,0" BorderBrush="#3498DB">
                            <Border.Effect>
                                <DropShadowEffect BlurRadius="12" ShadowDepth="3" Color="#D0D0D0"/>
                            </Border.Effect>
                            <StackPanel>
                                <Grid>
                                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Left">
                                        <TextBlock Text="{Binding Estado}" x:Name="txtEstado" Foreground="#3498DB" FontWeight="Bold" FontSize="11" VerticalAlignment="Center"/>
                                        <TextBlock Text="{Binding HoraRegistro}" Foreground="#95A5A6" FontSize="10" Margin="6,0,0,0" VerticalAlignment="Center" FontStyle="Italic"/>
                                    </StackPanel>

                                    <TextBlock Text="{Binding FechaEntrega, StringFormat={}{0:dd/MM}}" Foreground="#7F8C8D" FontWeight="Bold" HorizontalAlignment="Right" VerticalAlignment="Center"/>
                                </Grid>

                                <TextBlock Text="{Binding ClienteNombre}" FontSize="18" FontWeight="Bold" Margin="0,10,0,2" TextTrimming="CharacterEllipsis" Foreground="#2C3E50"/>
                                <TextBlock Text="{Binding Telefono}" Foreground="#5D6D7E" Margin="0,0,0,8" FontSize="13"/>

                                <Border BorderThickness="0,1,0,1" BorderBrush="#F2F4F4" Padding="0,8" Margin="0,5">
                                    <Grid>
                                        <StackPanel HorizontalAlignment="Left">
                                            <TextBlock Text="Anticipo Dejado:" FontSize="10" Foreground="#7F8C8D"/>
                                            <TextBlock Text="{Binding Anticipo, StringFormat={}{0:C}}" FontWeight="SemiBold" Foreground="#27AE60"/>
                                        </StackPanel>
                                        <StackPanel HorizontalAlignment="Right">
                                            <TextBlock Text="Saldo Pendiente:" FontSize="10" Foreground="#7F8C8D" HorizontalAlignment="Right"/>
                                            <TextBlock Text="{Binding SaldoPendiente, StringFormat={}{0:C}}" FontWeight="Bold" Foreground="#E74C3C" HorizontalAlignment="Right"/>
                                        </StackPanel>
                                    </Grid>
                                </Border>

                                <Border Background="#EBF5FB" CornerRadius="5" Padding="8" Margin="0,5">
                                    <StackPanel>
                                        <TextBlock Text="🌸 PEDIDO O COMPOSICIÓN:" FontSize="10" FontWeight="Bold" Foreground="#2980B9" Margin="0,0,0,2"/>
                                        <TextBlock Text="{Binding Descripcion}" FontSize="13" Foreground="#1B4F72" TextWrapping="Wrap" FontWeight="SemiBold"/>
                                    </StackPanel>
                                </Border>

                                <StackPanel Margin="0,5,0,10">
                                    <TextBlock Text="📍 Dirección / Destino:" FontSize="11" FontWeight="Bold" Foreground="#7F8C8D"/>
                                    <TextBlock Text="{Binding Direccion}" TextWrapping="Wrap" Height="35" FontSize="13" Foreground="#2C3E50" Margin="0,2,0,0"/>
                                </StackPanel>

                                <Button Content="Ver Detalles / Avanzar" 
                                        Click="btnVerDetalles_Click" 
                                        Background="#3498DB" Foreground="White" BorderThickness="0" Height="35" FontWeight="SemiBold" Cursor="Hand">
                                    <Button.Resources>
                                        <Style TargetType="Border">
                                            <Setter Property="CornerRadius" Value="6"/>
                                        </Style>
                                    </Button.Resources>
                                </Button>
                            </StackPanel>
                        </Border>

                        <DataTemplate.Triggers>
                            <DataTrigger Binding="{Binding Estado}" Value="En Preparación">
                                <Setter TargetName="CardBorder" Property="BorderBrush" Value="#F39C12"/>
                                <Setter TargetName="txtEstado" Property="Foreground" Value="#F39C12"/>
                            </DataTrigger>
                            <DataTrigger Binding="{Binding Estado}" Value="Listo para Entregar">
                                <Setter TargetName="CardBorder" Property="BorderBrush" Value="#27AE60"/>
                                <Setter TargetName="txtEstado" Property="Foreground" Value="#27AE60"/>
                            </DataTrigger>
                            <DataTrigger Binding="{Binding Estado}" Value="Entregado">
                                <Setter TargetName="CardBorder" Property="BorderBrush" Value="#7F8C8D"/>
                                <Setter TargetName="txtEstado" Property="Foreground" Value="#7F8C8D"/>
                            </DataTrigger>
                        </DataTemplate.Triggers>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
    </Grid>
</UserControl>