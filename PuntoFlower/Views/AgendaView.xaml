<UserControl x:Class="PuntoFlower.Views.AgendaView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:PuntoFlower.Views"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="1000">
    <Grid Background="#F5F5F5">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="White" Margin="10" CornerRadius="8" Padding="15">
            <Border.Effect>
                <DropShadowEffect BlurRadius="10" ShadowDepth="1" Color="#DDD"/>
            </Border.Effect>
            <DockPanel LastChildFill="False">
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="📅 Agenda de Pedidos" FontSize="24" FontWeight="Bold" VerticalAlignment="Center" Margin="0,0,20,0"/>
                    <Button Content="Todos" Width="80" Margin="5,0" Background="#3498DB" Foreground="White" BorderThickness="0" Cursor="Hand"/>
                    <Button Content="Para Hoy" Width="80" Margin="5,0" Background="White" BorderBrush="#DDD" Cursor="Hand"/>
                </StackPanel>

                <Button Content="+ Agendar Nuevo Pedido" DockPanel.Dock="Right" 
                        Click="btnNuevoPedido_Click" Width="180" Height="35" 
                        Background="#27AE60" Foreground="White" FontWeight="Bold" BorderThickness="0" Cursor="Hand"/>
            </DockPanel>
        </Border>

        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <ItemsControl x:Name="icPedidos" Margin="10">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <WrapPanel Orientation="Horizontal"/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>

                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Width="310" Background="White" Margin="10" CornerRadius="12" Padding="15">
                            <Border.Effect>
                                <DropShadowEffect BlurRadius="12" ShadowDepth="3" Color="#D0D0D0"/>
                            </Border.Effect>
                            <StackPanel>
                                <DockPanel>
                                    <TextBlock Text="{Binding Estado}" Foreground="#E67E22" FontWeight="Bold" FontSize="12"/>
                                    <TextBlock Text="{Binding FechaEntrega, StringFormat={}{0:dd/MM HH:mm}}" DockPanel.Dock="Right" Foreground="#7F8C8D" FontWeight="SemiBold"/>
                                </DockPanel>

                                <TextBlock Text="{Binding ClienteNombre}" FontSize="18" FontWeight="Bold" Margin="0,10,0,2" TextTrimming="CharacterEllipsis" Foreground="#2C3E50"/>
                                <TextBlock Text="{Binding Telefono}" Foreground="#5D6D7E" Margin="0,0,0,8" FontSize="13"/>

                                <Border Background="#EBF5FB" CornerRadius="5" Padding="8" Margin="0,5">
                                    <StackPanel>
                                        <TextBlock Text="🌸 PEDIDO:" FontSize="10" FontWeight="Bold" Foreground="#2980B9" Margin="0,0,0,2"/>
                                        <TextBlock Text="{Binding Descripcion}" FontSize="14" Foreground="#1B4F72" TextWrapping="Wrap" FontWeight="SemiBold"/>
                                    </StackPanel>
                                </Border>

                                <Separator Background="#F2F4F4" Margin="0,8"/>

                                <StackPanel Margin="0,0,0,10">
                                    <TextBlock Text="📍 Dirección:" FontSize="11" FontWeight="Bold" Foreground="#7F8C8D"/>
                                    <TextBlock Text="{Binding Direccion}" TextWrapping="Wrap" Height="35" FontSize="13" Foreground="#2C3E50" Margin="0,2,0,0"/>
                                </StackPanel>

                                <Button Content="Ver Detalles / Entregar" 
                                        Click="btnVerDetalles_Click" 
                                        Background="#3498DB" Foreground="White" BorderThickness="0" Height="35" FontWeight="SemiBold" Cursor="Hand">
                                    <Button.Resources>
                                        <Style TargetType="Border">
                                            <Setter Property="CornerRadius" Value="6"/>
                                        </Style>
                                    </Button.Resources>
                                </Button>
                            </StackPanel>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
    </Grid>
</UserControl>